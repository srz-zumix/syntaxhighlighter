- name: ci
  tag: master
  service: syntaxhighlighter
  type: serial
  steps:
    - command: ls

- name: deploy
  tag: master
  service: push
  type: serial
  steps:
    - command: 'curl -sSL https://raw.githubusercontent.com/codeship/scripts/master/deployments/git_push.sh | bash -s'

