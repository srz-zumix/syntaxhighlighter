- name: ci
  tag: master
  service: syntaxhighlighter
  steps:
    - command: ls
    - command: npm install && npm install -g gulp
    - command: gulp setup-project
    - command: gulp build --brushes=all --theme=default --output=./docs
